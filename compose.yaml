name: marketplace-api-412
services:
  products-api:
    container_name: products-api-412
    image: products-api-412
    build:
      context: .
      dockerfile: ProductsApi/Dockerfile
    ports:
      - "5032:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      DB_CONNECTION: ${PRODUCTS_DB_CONNECTION}
    depends_on:
      - products-migrations
    restart: always
  
  products-db:
    container_name: products-db-412
    image: postgres:18.4-alpine3.23
    ports:
      - "5033:5432"
    environment:
      POSTGRES_DB: ${PRODUCTS_DB_NAME}
      POSTGRES_USER: ${PRODUCTS_DB_USER}
      POSTGRES_PASSWORD: ${PRODUCTS_DB_PASSWORD}
    volumes:
      - products-data:/var/lib/postgresql
    restart: always
  
  products-migrations:
    container_name: products-migrations-412
    image: products-migrations-412
    build:
      context: .
      dockerfile: ProductsMigrations/Dockerfile
    environment:
      DB_CONNECTION: ${PRODUCTS_DB_CONNECTION}
    depends_on:
      - products-db
    restart: on-failure

volumes:
  products-data:
    

